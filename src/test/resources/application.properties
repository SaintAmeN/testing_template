spring.datasource.url=jdbc:mysql://localhost:3306/test_javazda75_spring_testing?serverTimezone=Europe/Warsaw&createDatabaseIfNotExist=true
spring.datasource.password=root
spring.datasource.username=root

# update - generuje tabele i aktualizuje
# validate - waliduje, nie generuje
# create - usuwa baze i tworzy now?
# create-drop - stworzenie bazy, na koniec wyczyszczenie
spring.jpa.hibernate.ddl-auto=create-drop
